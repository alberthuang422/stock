# 工作交接文档（2026-08-31 全天）

> 本文件是每日收尾交接文档，汇总当日工作产出、关键结论、git 状态与遗留待办，供后续会话快速恢复上下文。逐日明细见 `.workbuddy/memory/YYYY-MM-DD.md`，长期要点见 `.workbuddy/memory/MEMORY.md`，报告检索入口为 `README.md`。

## 一、当日工作全景（8/31）

| # | 主题 | 核心结论（一句话） |
|---|---|---|
| 60 | 日线 MACD 死叉 × 4h RSI(14) 30-35 超卖 → 买入胜率回测（SOXX/NVDA/XAUUSD/QQQ） | 合并 20 信号：T+1 胜率 70% / T+5 73.7%（中位 +2.5%）/ T+10 68.4% / **T+20 掉回 52.6%** → **捕捉短线反抽、非趋势反转**；对照组仅日线死叉 n=87、T+5~T+20 胜率 70-72% p<0.01 → **主效果是死叉本身，4h RSI 30-35 过滤把样本砍 8 成、只到边缘显著（锦上添花的择时确认）** |
| 60-拆 | 分标的信号与稳定性 | 信号数：XAUUSD 7 / NVDA 5 / QQQ 5 / SOXX 3；黄金最稳（T+1 7/7、T+20 5/7，乘 2024-26 黄金大牛市背景）；单笔风险示例 SOXX 2025-03-28 关税周信号 T+5 −12.7% |
| 60-快照 | **当前买点信号** | XAUUSD 正处信号窗（08-31 收盘：日线刚死叉 DIF 102.5<DEA 103.1 + 4h RSI 32.4）→ **09-01 开盘即回测买点**；SOXX 水下接近未触发；NVDA/QQQ 金叉后无信号 |
| 61 | XAUUSD × US10Y 滚动相关性（轻量专题，未建报告目录） | 近 60/40/20 日静态相关（补齐口径）**r = −0.11 / −0.31 / −0.26**（官方口径 −0.07/−0.23/−0.15，低估负相关）；近 40 日 p=0.05 边缘显著，60/20 日不显著；**8 月黄金独立上涨（近 20 日 +11.0%）与利率（+6bp）弱关联**，是"弱负相关"而非利率避险/抗通胀主导；60 日滚动轨迹 5 月深负（−0.44）→ 6 月末收敛 → 7 月小幅回正 → 8 月回落 −0.22~−0.12 |
| 数据 | Yahoo 官方 interval=4h 直连跑通（无需 CDP） | **日线 range=max 返回"月度采样"假数据**（~302 行、日期全在每月 1 号）→ 必须显式 `period1/period2` 拉全量；**4h 仅支持 range=2y（上限约 730 天）**、美股 2 根/日（ET 09:30/13:30）；XAUUSD 无现货代码用 **GC=F（COMEX 黄金期货）** 代理；模板 `scripts/fetch_macd_soxx_nvda_xau_qqq.py` |
| 数据 | DGS10 官方滞后补录 | FRED DGS10 滞后至 08-27，**08-28 黄金 −2.86% 大跌日不在官方序列**；用 Yahoo ^TNX（query2 range=5d 避开 query1 限流）补录 08-28 收盘 4.76%（+9bp）→ **结论一律以补齐口径为准** |

## 二、重要方法论决策 / 新增约定（8/31）

1. **Yahoo 日线/盘中直连规则**（60 号实证，替代 CDP）：日线必须显式 `period1/period2`（range=max 是月度采样假数据）；4h 只能用 range=2y（国际可用上限 ~730 天）；XAUUSD 无现货代码时用 GC=F 代理。详见 MEMORY.md「数据与拉数」。
2. **拆股标的回测结算规则**：收益用 adj_close 后复权价结算、信号用原价判定（NVDA 2024 1:10 拆分，不复权会虚造假收益）。
3. **filter 效果必须做对照实验**：本次 4h RSI 超卖过滤"看起来有效"，对照仅日线死叉后真相是主效果在死叉本身——单组回测胜率不构成证据，必须与无过滤基线对比显著性（60 号的 n=20 vs n=87 是标准做法）。
4. **FRED 滞后缺口用 ^TNX 补齐**：官方序列缺最新交易日时用 Yahoo ^TNX 补录并显式标注口径；结论以补齐口径为准（官方口径会系统性低估近期负相关）。
5. **summarize 函数对已是 % 的收益严禁再 ×100**（双倍缩放坑，本次踩坑）；术语注释 annotate_terms 需先保护 data-tip 内容防嵌套 span。
6. **延续铁律**：60 日滚动主口径/显著性三档+p 值/参数图例/R 与 β 同列/单位陷阱 corr ×100 入库 build ÷100/收益原始全序列先算再 merge/README 登记前先 ls 校验/术语悬停浮窗禁速查表/红涨绿跌+Okabe-Ito/交付克制/尾 bar 体检剔除不完整 bar。

## 三、当日产出清单（8/31）

- **报告**：`reports/60_MACD死叉_4hRSI超卖_胜率回测/index.html`（主报告，README 已登记）
- **脚本**（scripts/）：`fetch_macd_soxx_nvda_xau_qqq.py`（Yahoo 直连拉数模板）、`analyze_60_*.py`（回测）、`build_60_*.py`（报告构建）、`test_render_60_macd.cjs`（无头渲染验证：canvas 非空白+零 console error）、`gold_us10y_corr.py`（61 号）
- **结果**（results/）：`60_*.json`（回测明细）、`gold_us10y_corr.json`（含 `recent_with_tnx_0828` 修正块）
- **数据**（data/）：`soxx/`、`nvda/`、`qqq/` 各新增 4h CSV；`xauusd/`（1D+4h）
- **README.md**：60 号已登记（分类 3 宏观利率与资产联动）

## 四、git 状态与本次提交（2026-09-01 00:2x，每日交接）

**前情**：8/31 已按主题提交——白天 `f5a9b00`（automation 执行记录第八次）→ 晚间 `0f8e508`（60 号回测）→ 23:51 `7878bf0`（61 号 XAUUSD×US10Y）。**本次交接前工作区仅剩**：`.workbuddy/artifacts/overview.md`（本次覆盖）、`.workbuddy/memory/2026-08-31.md`（8/31 日志，未入库）、`.workbuddy/memory/MEMORY.md`（60/61 号要点已补入）、`Temp/`（中间产物 dgs10_latest.csv + gold_us10y_chart.json，按惯例不提交、不删除）。

**本次动作**（用户明确要求"提交到 github 即可"）：
1. 覆盖更新 `overview.md`（8/31 全天汇总，本文档）
2. 单 commit 收尾（交接文档 + 8/31 日志 + MEMORY 更新）→ push（ssh 22 端口被 reset，走 **ssh.github.com:443**）→ ls-remote 验证远端与本地一致

**安全说明**：全程仅 add/commit/push；无删除/clean/reset/force；Temp/ 及任何未跟踪文件不做处理；全部改动可 git 回滚。

## 五、重要过程 / 踩坑（8/31）

- **Yahoo 日线 range=max 月度采样假数据坑**（最深）：首次用 range=max 拿到月度采样（302 行≈25年×12月，日期全在每月 1 号），MACD 死叉全落月初、回测全废；改显式 period1/period2 后正常。已验证直连 query1.finance.yahoo.com 可用、无需 CDP。
- **4h 窗口上限**：range=max 对 4h 无效，必须 range=2y（~730 天，2024-09 起），样本天然受限——60 号合并仅 20 信号的重要原因。
- **NVDA 拆股收益污染**：不用 adj_close 会给 2024-03 前后信号虚造 10 倍假收益。
- **summarize 双倍缩放**：收益明细已是 % 后 summarize 再 ×100，产生双倍缩放假收益（60 号踩坑）。
- **annotate_terms 嵌套 span**：定义文本里的术语词被二次注释成嵌套 span → 先正则保护 data-tip 内容再注释正文，最后还原。
- **^TNX 限流**：query1 对 ^TNX 短窗口易限流，用 query2 + range=5d 规避（61 号实证）。
- **未核实/遗留标注**：61 号黄金大涨日内具体资金面/避险归因未核实（仅从价格-利率相关性推断"独立行情"）；60 号 SOXX 2025-03-28 关税周案例为单一事件的机制解释。

## 六、用户规则（长期，详见 ~/.workbuddy/MEMORY.md + 项目 MEMORY.md）

- git 提交格式 `yyyy-mm-dd  msg: （简短内容，≤50字）`、按主题分 commit、**默认只本地 commit 不 push（用户明确要求才 push，本次已获授权）**。
- **高权限自我约束（用户本次明确叮嘱）**：不做删除/clean/reset/force 等破坏性操作，全部改动可 git 回滚；push 前先确认工作区状态。
- 交付必须产物展示（present_files + 自动打开预览）；交付不附图；HTML 报告出完后 CDP 无头验证兜底。
- 术语悬停浮窗不用速查表；红涨绿跌 + Okabe-Ito 色弱安全（叠线型/符号）；报告表格参数图例；R 与 β 同列。
- 结论必须先核实数据、无法核实标注「未核实」；评审四段式；相关性 60 日滚动主口径；交付克制（只做用户明确要求的内容）。
- CDP 仅在推特/X 抓取或常规手段不可行时启用，用完必关。

## 七、遗留待办 / 下次继续

- **★ XAUUSD 信号窗落地**：09-01（今日）开盘是 60 号主口径买点（日线刚死叉 + 4h RSI 32.4），可挂单观察 T+5（胜率最高档 73.7%、27 年前 5 个信号全对）；SOXX 水下接近未触发可盯 4h RSI。
- **9/4 8 月非农分水岭**（沿用）：若为负（连续 2 月）→ 按先例=衰退起点解读，9/15-16 FOMC 加息概率从 55.7% 大幅回吐；落地后复核 10Y-2Y/10Y-30Y 精确值（8/28 后未核实）。
- **58 号主报告修订**（沿用）：仍是 CL 单口径旧版，须补 XLE 双口径表述（"脱油价锚、未脱能源板块锚"，CF 对 XLE β≈1.1）。
- **57 附 p 值 bug**（沿用）：主报告 index.html 手写 t CDF 错误待修（附版 scipy 已对）。
- **9 月 ASO ONI 发布后**（沿用）：复核残差表 onset 后是否重新扩张；ASO≥+1.5 则上调 2015 剧案权重。
- **待验证**（沿用）：拉 NG=F + 谷物期货（玉米/大豆/棕榈）验证 CF 成本链与厄尔尼诺→谷物→化肥传导。
- **52 号 MCD 操作跟踪**（沿用）：4% 反弹目标（260→270.5）、收盘止损 <255/<253。
- **45/31 号深化、GE 周线高位死叉、VIX 中期选举窗口、富途选股器 450 池、56 号 CCL 等 <30 档买点**（沿用，详见 8/28-8/30 交接）。
- **Temp/**：`dgs10_latest.csv`、`gold_us10y_chart.json` 为中间产物，保留未跟踪，需时 FRED/^TNX 可随时重拉。