# 2026-08-22

## futu 期权墙八标的（ABBV/GILD/LLY/JNJ/KO/SBUX/CSCO/AMZN，2026-09-18 到期）
- 数据管线：`quote_option_expiration_date` → `quote_option_chain`（start=end=单到期日）→ `quote_stock_quote` 批量拿 OI/IV/希腊字母（≤400 code/批，3 批）。链文件 `results/chain_{TK}_20260918.json`（8 只共 1074 合约），行情大响应落盘 tool-results/*.txt 由 `scripts/compute_option_walls.py` 正则解析（1074/1074 匹配）。
- 结果：`results/option_walls_20260918.json` + `option_walls_near_20260918.json`；报告 `reports/20_期权墙八标的/index.html`（build_option_walls_report.py + test_render_option_walls.cjs 渲染测试通过）。
- 核心结论（Call墙/近场Put墙/MaxPain/PCR）：ABBV 280/220/230/0.42；GILD 150/140/135/0.45；LLY 1320/1050/1060/0.98；JNJ 290/220/230/0.55；KO 95/77.5/80/0.56；SBUX 110/105/105/1.25；CSCO 80/100/95/0.73；AMZN 300/220/245/0.65。
- 口径坑：全场极值被深度价外老仓位污染（LLY Put 墙 520、AMZN Put 墙 160 为遗留仓位；CSCO Call 墙 80 为深度价内股票替代）→ 补充近场口径（Put≥0.8×spot、Call≤1.15×spot 内取最大 OI）。MaxPain=Σ CallOI·max(0,S−K)+Σ PutOI·max(0,K−S) 最小值。
- **build 脚本 bug 教训**：HTML JS 里用了 Python 端的 `ORDER` 数组但只注入了 DATA → 页面报 `ORDER is not defined`、全部图表 NO-CANVAS。凡 JS 引用的变量必须在注入清单里（本次加 `__ORDER_JSON__` 占位修复）。渲染测试再次证明必要。
