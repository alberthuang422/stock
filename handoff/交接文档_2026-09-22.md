# 工作交接文档（2026-09-22 全天）

> 本文件是每日收尾交接文档，汇总当日工作产出、关键结论、git 状态与遗留待办，供后续会话快速恢复上下文。
> 逐日工作明细在本地 `.workbuddy/memory/2026-09-22.md`（`.workbuddy/` 已移出版本控制，仅本地），长期要点见 `.workbuddy/memory/MEMORY.md`，报告检索入口为 `README.md`。
> 上一份：`handoff/交接文档_2026-09-21.md`（commit af4425d）。

## 一、当日全景（9/22 全天）

### 主题一：data.csv 波动率分析（未入库，独立数据）
- 数据：用户下载的独立标的周线，`Downloads/data.csv`，2869 根周线（bar1=4.92 → 峰 61.20(bar164, +1144%) → 谷 2.71(bar716, −95.6%) → 收盘 17.47）。
- 周线年化口径（√52）：全样本 RV=38.0%；前/中/后 1/3 = 49%/32%/31%；当前 20 周 RV=26.4%（历史 P28）、60 周 25.4%、EWMA(0.94)=27.7%；RV 分布 P10/P50/P90 = 20.5/32.6/54.1。无期权链 → IV 不可直接算。
- 用户补充标的性质：**锚定某农产品价格的标的**。结论：IV=25% 对农产品合理且常态（农产品期权 IV 中枢 20-30%、负 VRP 常见）；当前 IV≈RV 贴平，定价自洽；"价格波动大"是趋势/极端周/几十年回撤，不进近端期权定价。
- 审慎点：−95.6% 回撤超出原生态农产品期货典型幅度（<70-80%），标的可能含杠杆/合成结构——**性质未核实**。
- 脚本：`Temp/vol_analysis_data_csv.py`（主统计，√52）、`vol_analysis2_data_csv.py`（分段/路径）、`vol_iv_check.py`（IV-RV 对照）、`vol_chart_data_weekly.py`（图表数据）。输出 txt 同名。**均为临时脚本，未迁入 scripts/，数据亦未入库（用户下载的独立数据，不属仓库资产）**。

## 二、方法论沉淀（9/22 新增）

1. **外部独立数据不强制入库**：用户下载、非仓库产生、无长期引用价值的分析（本日 data.csv 波动率），脚本留 Temp/ 即可，交接文档记录结论与数据性质，不 commit 数据本身（避免污染仓库）。
2. **删除文件处置流程（沿用并细化）**：git 报 deleted → ①查历史备份（git log --follow + git show HEAD:file 可回滚确认）→ ②查全仓引用（README + grep 全仓）→ ③无引用且确属历史独立产物 → 确认删除入库，不 restore（保 git 历史可回滚）。本日 options-strategy.html 即按此处置。

## 三、产出清单

| 类别 | 路径 | 说明 |
|---|---|---|
| 交接 | `handoff/交接文档_2026-09-22.md` | 本文档 |
| 分析脚本（临时，不入库） | `Temp/vol_analysis_*.py` ×4 + `Temp/vol_*_out.txt` | data.csv 周线波动率统计（√52 口径） |
| 分析数据（不入库） | `Downloads/data.csv` | 用户下载的 2869 根周线 |

## 四、git 状态与本次提交（2026-09-23 00:1x，每日交接）

**前情**：9/21 交接已推送（远端 = 本地 = `af4425d`）；9/22 白天无任何新 commit（git log --since 全天为空）；但工作区有 **1 处未提交删除**。

**本次动作**（用户每日授权"提交到 github 即可"，全程仅 add/commit/push，无历史改写/force/clean/reset，无删除/修改任何文件）：
1. 核验 9/22 产出（git log / find 时间戳 / git status 三查 + 读 .workbuddy/memory/2026-09-22.md）
2. **options-strategy.html 删除处置**：git log 有历史备份（aadd106, 2026-08-21），磁盘确已删除，README + 全仓无任何引用 → 属孤立历史产物，确认删除入库（不 restore），git 历史可随时回滚
3. 撰写本交接文档（handoff/交接文档_2026-09-22.md）
4. commit（删除入库 + 交接文档，分主题 2 个 commit）
5. push origin/main
6. fetch 后 rev-parse 验证远端与本地一致（ahead/behind=0/0）

**安全边界声明**：本次全程未删除任何新的数据/脚本/报告文件，未修改任何文件内容，未 reset/force/改写历史；唯一的删除入库是对已删除孤立文件的确认入账（git 历史保留备份）。

## 五、重要过程 / 踩坑

1. **git 环境噪音（沿用）**：git/stat 命令 stderr 带 crashpad ERROR（`CreateFile: 系统找不到指定的文件`），不影响结果，勿误判失败。
2. **PowerShell 通道本次完全无输出**（Write-Output / git 均 stdout 空），疑似通道故障 → 改用 Bash + git.exe 绝对路径完成全部 git 操作，正常。若再次遇到 PS 空输出，直接切 Bash。

## 六、用户规则 / 偏好（例行记录，未变更）

- 交付克制：只做用户明确要求的内容；个股/主题分析默认 HTML 研报交付（reports/ 编号体系），对话里只附摘要。
- git：提交格式 `yyyy-mm-dd  msg: ≤50字`；主题相关分 commit；本地 commit，push 需明确要求（本项目已每日授权）。**高权限注意：不执行敏感/破坏性操作，删除或修改文件先经 git 处理保证可回滚**。
- 数据核实纪律：结论必先核实；无法核实标注"未核实"；绿色弱 → Okabe-Ito 色板 + 非颜色线索；红涨绿跌；差值优先绝对数值；气候/库存类水平×水平相关必做去趋势复核。

## 七、遗留待办（下次会话从这里继续）

1. **【自动化确认，延续】热榜 RSI（f78907c8）+ 支撑阻力（dca939ab）PAUSED**：问用户是否恢复（若恢复，热榜失败率 35.7% 六连>30% 的 OTC ADR 剔除细则需先拍板）。
2. **【文件迁移，延续】Temp/ 三文件迁出**：plate_bucket.json、build_rsi_eval.py、resistance_picks.json（自动化持久依赖）→ 建议移 scripts/ 或 config/ 并改 daily_hot_rsi.py 与支撑阻力脚本路径引用，防清 Temp 再次破坏（9/15 已误删过一次、已恢复）。
3. **【外部目录，延续】农业线归档目录 `C:\Users\Administrator\Desktop\农业\`**：已迁出仓库，git 无权跟踪，跨机同步/备份策略由用户手动管理。
4. **【数据资产，延续】data/cl_contracts/、data/ho_contracts/ 本机缺失未跟踪**（RB 已重建）；跨机同步策略待用户确认。
5. **【持仓/市场跟踪，延续】NOV-JAN 空仓时间止损（10 月上旬）**；做空月差候选 M2-M3；11/3 中期选举分水岭；霍尔木兹流量回升观察（日均能否回 800-900 万桶）；美方终战方案窗口（制裁缓解条款）。
6. **【可选】9/22 data.csv 波动率分析**：若用户要正式成文，可基于 Temp/vol_* 脚本产出 HTML 报告；标的性质（杠杆/合成结构）待核实后再定稿。