# 工作交接文档（2026-09-20 全天）

> 本文件是每日收尾交接文档，汇总当日工作产出、关键结论、git 状态与遗留待办，供后续会话快速恢复上下文。
> 逐日工作明细在本地 `.workbuddy/memory/2026-09-20.md`（`.workbuddy/` 已移出版本控制，仅本地），长期要点见 `.workbuddy/memory/MEMORY.md`，报告检索入口为 `README.md`。
> 上一份：`handoff/交接文档_2026-09-19.md`（commit b9b817c）。

## 一、当日全景（9/20 全天，单主题：项目范围收敛·农业线迁出）

9/20 白天为**项目范围治理**日，无新增分析/报告产出。应用户要求，将 stock 项目内全部**农业相关内容（数据/价格/脚本/报告/记忆痕迹）迁移归档至外部 `C:\Users\Administrator\Desktop\农业\` 目录**，本项目从此只保留股票相关。改动已全部合入 commit `4a31eca`（183 files changed，−990,071 行，系统本周最大单次变更）。

| 类别 | 动作明细 |
|---|---|
| 报告 | 删除残留农业报告 `reports/86_三品种可贸易库存三口径_20260912`；清空 `reports/00_报告治理_20260905` 中 5 条农业报告索引行（57/58/59/68/72 号） |
| 交接文档 | 从 `handoff/*.md` 十份交接文档摘除农业章节（农业数据资产、ENSO×农产品、白糖 CFTC、94/95/96/97 号、关税×农产品、2019 玉米归因等） |
| 记忆层 | 10 份日账外科式删除农业段落；`PLAYBOOK.md` 第 5/6/7 章（USDA PSD API/CFTC COT/ENSO）合并为「气候归因方法论（通用）」保留通用铁律（水平×水平伪相关、去趋势/差分三口径、块自助复核 p），删农业专属接口与商品码；自动化记忆 `automation-1787497964749/memory.md` 32 处替换；`MEMORY.md` 本身 0 命中无需动 |
| 数据/脚本 | 农业数据目录（data/agri|enso|cotton|corn|cane|cf|adm|agco|bg|ctva|dba|de 等）、results/ 糖相关 JSON/CSV、scripts/ 农业脚本（build_report96/cftc_sugar/enso_europe 等）全部出库 |
| 仓库卫生 | Temp/ 清空 49 个一次性脚本（保留 3 个自动化硬依赖 + 未跟踪 ice_probe.py/_t97.js） |

**保留项（合法，非遗留污染）**：宏观日历类正向引用「9/11 USDA 作物产量」「黑海停火斡旋」「ENSO OND 强度确认」（属股票/宏观跟踪项）；热榜 RSI 的 GICS「农业投入品」桶标签；83 号报告「拉尼娜相关寒潮」（能源需求侧）；UNP 报告「谷物运输」（铁路业务板块）；`.gitignore 防 USDA key 入库`安全说明。

**备份**：改动前原件存于 `Temp/_bak_agri4/`（报告/交接文档）与 `Temp/_bak_mem_agri/`（记忆文件），如用户日后需要可回查。

## 二、方法论沉淀（9/20 新增）

1. **目录级迁移 = 低风险治理手段**：跨目录归档而非逐条删除，配合内存备份（Temp/_bak_*）与 git 历史，任一层级可回滚；git 的删除以 commit 呈现，本身即回滚点。
2. **记忆层擦除的「合法保留」清单法**：清理项目范围时先把「正向引用/行业标签/板块业务」与「主题污染」分类，前者保留并显式列出，避免误伤宏观跟踪项。

## 三、产出清单

| 类别 | 路径 | 说明 |
|---|---|---|
| 交接 | `handoff/交接文档_2026-09-20.md` | 本文档 |
| 备份（未跟踪） | `Temp/_bak_agri4/`、`Temp/_bak_mem_agri/` | 农业线改动前原件，本地保留可回查 |
| 迁移目标（仓库外） | `C:\Users\Administrator\Desktop\农业\` | 农业数据/脚本/报告归档目录 |

## 四、git 状态与本次提交（2026-09-21 00:0x，每日交接）

**前情**：9/20 白天 1 个 commit `4a31eca`（农业线清理）已本地入库**未推送**（当日遵「只本地提交」规则）；9/19 交接 `b9b817c` 已推送（远端 = b9b817c）。

**本次动作**（用户每日授权"提交到 github 即可"，全程仅 add/commit/push，无历史改写/force/clean/reset，无删除——删除动作 9/20 白天已完成并经 git 记账）：
1. 撰写本交接文档（handoff/交接文档_2026-09-20.md）
2. commit 交接文档（单独成 commit）
3. push origin/main（`4a31eca` + 本交接 = 2 个 commit 送达远端）
4. ls-remote / rev-parse 验证远端与本地一致（ahead/behind=0/0）

## 五、重要过程 / 踩坑

1. **⚠️ 超大单文件教训（9/20 复核确认）**：9/15 曾因 190MB 单文件触发 GitHub GH001（100MB 上限），已用 filter-branch 只改写未推送区间移除并 gitignore——9/20 清理农业大文件（ANRPC xlsx、wasde xls）时再次确认此规则：大文件禁直接入库，本地保留 + gitignore。
2. **git 环境噪音**：所有 git/stat 命令 stderr 带 crashpad ERROR（`CreateFile: 系统找不到指定的文件`），不影响结果，勿误判失败。
3. **本机 PowerShell PATH 不含 git**（09-20 备忘）：调用前须 `$env:Path += ";C:\Users\Administrator\.workbuddy\binaries\PortableGit\versions\1.2.0\cmd"`；Bash 工具内 git 可用（本次即走 Bash）。

## 六、用户规则 / 偏好（例行记录，未变更）

- 交付克制：只做用户明确要求的内容；个股/主题分析默认 HTML 研报交付（reports/ 编号体系），对话里只附摘要。
- git：提交格式 `yyyy-mm-dd  msg: ≤50字`；主题相关分 commit；本地 commit，push 需明确要求（本项目已每日授权）。**高权限注意：不执行敏感/破坏性操作，删除或修改文件先经 git 处理保证可回滚**（本次无新增删除/修改，工作区自始干净）。
- 数据核实纪律：结论必先核实；无法核实标注"未核实"；绿色弱 → Okabe-Ito 色板 + 非颜色线索；红涨绿跌；差值优先绝对数值；气候/库存类水平×水平相关必做去趋势复核。

## 七、遗留待办（下次会话从这里继续）

1. **【自动化确认，延续】热榜 RSI（f78907c8）+ 支撑阻力（dca939ab）PAUSED**：问用户是否恢复（9/20 核实仍 PAUSED；若恢复，热榜失败率 35.7% 六连>30% 的 OTC ADR 剔除细则需先拍板）。
2. **【文件迁移，延续】Temp/ 三文件迁出**：plate_bucket.json、build_rsi_eval.py、resistance_picks.json（自动化持久依赖）→ 建议移 scripts/ 或 config/ 并改 daily_hot_rsi.py 与支撑阻力脚本路径引用，防清 Temp 再次破坏（9/15 已误删过一次、已恢复）。
3. **【外部目录，新增】农业线归档目录 `C:\Users\Administrator\Desktop\农业\`**：已迁出仓库，git 无权跟踪，跨机同步/备份策略由用户手动管理；如需回看历史版本走 git 历史或 Temp/_bak_*。
4. **【数据资产，延续】data/cl_contracts/、data/ho_contracts/ 本机缺失未跟踪**（RB 已重建）；跨机同步策略待用户确认。
5. **【持仓/市场跟踪，延续】NOV-JAN 空仓时间止损（10 月上旬）**；做空月差候选 M2-M3；11/3 中期选举分水岭；霍尔木兹流量回升观察（日均能否回 800-900 万桶）；美方终战方案窗口（制裁缓解条款）。