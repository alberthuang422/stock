# 工作交接文档（2026-09-09 全天）

> 本文件是每日收尾交接文档，汇总当日工作产出、关键结论、git 状态与遗留待办，供后续会话快速恢复上下文。
> 逐日工作明细在本地 `.workbuddy/memory/2026-09-09.md`（`.workbuddy/` 已移出版本控制，仅本地），长期要点见 `.workbuddy/memory/MEMORY.md`，报告检索入口为 `README.md`。

## 一、当日工作全景（9/9）

| # | 主题 | 核心结论（一句话） |
|---|---|---|
| 自动化 | 每日 07:00 热榜 RSI 日报（第八次，15:11 commit 7db8d26） | 成功：Top500 → 过滤后 405 只（price>500:33 / 中概:23 / ADR:39）→ RSI **有效 270 只（重试后仍失败 135/405，失败率 33.3%，经 7/8 两日短暂达标后回落至 >30% 区间）**；中位 RSI 48.8、超卖<30 共 9（TJX 19.7/SYK 24.7/RCL 25.2/PL/CDNS/STZ/NCLH/MAR/MGM）、超买≥70 共 7（MPC 80.8/VLO 79.3/PSX 77.2/DINO/BLTE/MOS/SWKS）。token refresh 自动续期 OK。产出 reports/hot_rsi_eval_20260909.html + hot_rsi_latest.html |
| 数据核查 | **能源 API 数据核查**（用户："查一下能源 api 数据"，对话产物，脚本 Temp/check_wti_merge.py） | ①项目内**无 EIA API key**（环境变量仅 USDA_FAS_API_KEY）；EIA 官方端点实测：api.eia.gov → 403（需 key）、ir.eia.gov WPSR → 302 CloudFront 签名超时、dnav/pet hist_xls → 200 但 size=0（WAF 空 body 拦截，需真实浏览器）。②**免 key 可用通道 = FRED 直连**：DCOILWTICO 可用并更新至 **2026-09-01（$91.48）**、DCOILBRENTEU $96.02；HHNGSP/NGAS/DGASUS1/WGFUPUS2/WPSR 等均返回 HTML（series 不存在），仅美元价格类可用。③**数据修复**：本地 `data/wti.csv` 停于 08-11，重叠区 10221 天与 FRED 0 mismatch，已用 FRED 补齐 15 个交易日 → 现至 **08-11~09-01（$84.77→$91.48，+7.9%）** |
| 数据修复 | **data/wti.csv 补齐**（同上，已随热榜 commit 7db8d26 入库） | WTI 日线缺口修复，全程同一 FRED 源，重叠区无偏差。**定论：后续能源价格更新一律走 FRED 直连（脚本可复用），EIA 官方 API/CSV 需 API key 或真实浏览器（WAF），不建议 curl 直抓** |

## 二、重要方法论决策 / 新增约定（9/9）

1. **EIA 官方通道不可用 → FRED 直连为能源价格默认通道**：`api.eia.gov` 需 key（项目无）、WPSR 与 dnav CSV 被 CloudFront WAF 空 body 拦截（curl 不可行，真实浏览器兜底）；FRED `graph/fredgraph.csv?id=DCOILWTICO|DCOILBRENTEU` 免 key 直连可用。已更新至 09-01。**天然气/成品油库存类 FRED series 也不可用，此类数据后续需另找通道（若 WPSR 实为 EIA 库存关键，可能需真实浏览器或 API key 方案，待用户定夺）**。
2. 延续铁律：60 日滚动主口径、红涨绿跌+Okabe-Ito 叠符号、交付克制、高权限下先 git 处理可回滚。

## 三、当日产出清单（9/9）

- **报告**（reports/）：
  - `hot_rsi_eval_20260909.html`（自动任务第 8 日）+ `hot_rsi_latest.html`
- **脚本**（Temp/，随热榜 commit 入库）：`Temp/check_wti_merge.py`（WTI 与 FRED 重叠校验+补齐）
- **数据/结果**：`data/wti.csv`（FRED 补齐至 09-01）、`data/rsi14_hot_20260909.csv`、`results/rsi14_hot_20260909.json`（270 条）；Temp/hot500_hot_filtered/plate_bucket_20260909.json、`Temp/fred_wti_full.csv`（10610 行 FRED 全史）、`Temp/daily_hot_rsi_20260909_run.log`（25 行收尾日志）
- **对话产物**（要点在本地 .workbuddy/memory/2026-09-09.md，不入 git）：能源 API 数据核查全流程结论
- **无新增报告编号**（今日为数据核查+自动日报，无个股深分产出）

## 四、git 状态与本次提交（2026-09-10 00:1x，每日交接）

**前情**：9/9 白天 1 个 commit 已入库——`7db8d26`（15:11 热榜日报，12 文件，含 data/wti.csv 补齐、Temp/check_wti_merge.py、fred_wti_full.csv 等）；远端 origin/main 停在 `d521463`（9/8 交接），本地领先 1 个 commit 未推。本轮开始时唯一未提交改动 = `Temp/daily_hot_rsi_20260909_run.log`（25 行收尾日志，7db8d26 提交时为空壳 20 行，15:11 后追加 5 行完成态内容）。

**本次动作**（用户每日授权"提交到 github 即可"）：
1. commit `a3a6d0e` Temp 热榜日志收尾（补齐空壳文件内容）
2. 撰写本交接文档 + commit
3. push origin/main（白天 1 + 本次 2 = 3 个 commit）
4. ls-remote / rev-parse 验证远端与本地一致（ahead/behind=0/0）

**安全说明**：全程仅 add/commit/push；无删除/clean/reset/force/checkout；全部改动可 git 回滚。

## 五、重要过程 / 踩坑（9/9）

1. **热榜失败率八连序列回落后反弹**：33.7 → 34.0 → 36.8 → 33.2 → 33.8 → 35.5 → **23.9 → 33.3**。今日失败 135 只中仍含大量大盘/**OTC ADR**（IONQ/RGTI/SMCI/MRNA/COIN/MCD 等知名票也失败，更值得注意——失败名单中包括主流大盘票，可能非纯 OTC 覆盖问题，需观察是否与数据源瞬时限流有关；OTC ADR 剔除规则细分仍待拍板）。
2. **EIA WAF 空 body 坑**：dnav/pet `hist_xls/*.xls` 返回 200 但 size=0，属 CloudFront 签名/WAF 拦截——**必须校验文件大小/内容**，不能只看 HTTP 200。ir.eia.gov 302 到带签名 Policy 的 URL 同样抓不到。
3. FRED series 需逐条实测：同为 FRED 通道，价格类（DCOILWTICO/DCOILBRENTEU）可用，库存/产量类（WGFUPUS2/WPSR/DGASUS1）返回 HTML 页面（series id 不存在或不可用）——**勿假设 FRED 全品类可用**。

## 六、用户规则 / 偏好（例行记录，未变更）

- 交付克制：只做用户明确要求的内容；个股分析默认 HTML 研报交付（reports/ 编号体系）。
- git：提交格式 `yyyy-mm-dd  msg: ≤50字`；主题相关分 commit；本地 commit，push 需明确要求（本项目已每日授权）。
- 数据核实纪律：结论必先核实，无法核实标注"未核实"；绿色弱 → Okabe-Ito 色板 + 非颜色线索。
- 高权限注意：不执行敏感/破坏性操作，删除或修改先 git 处理可回滚。

## 七、遗留待办（下次会话从这里继续）

1. **热榜 RSI 失败率 33.3% 反弹回 >30% 区间**：今日失败名单含大量主流大盘票（IONQ/RGTI/SMCI/MRNA/COIN/MCD/BA/PFE/JPM 等 135 只），需区分"数据源覆盖（OTC ADR）"与"瞬时限流/接口问题"两类成因；OTC ADR 剔除细则与失败重试策略待拍板。
2. **能源数据通道缺口**：EIA 官方 API/WPSR/dnav CSV 均不可 curl 直抓（需 key 或真实浏览器）；FRED 仅覆盖美元价格类。**美国原油/成品油库存周度数据（WPSR、WGFUPUS2 等）当前无可用免 key 通道**，若需要后续可能需真实浏览器抓 EIA 或申请 API key。
3. **霍尔木兹**：Dubai M1-M2（$12 最陡）、中国 to-be 到货、波斯湾浮仓月度跟踪；第三级触发 = Q4 旺季×中国补库×SPR 见底（M1-M2 重上 $8-10）；布伦特 $97.5 逼近 $100 与 OPEC+ 反应。
4. **宏观地缘观察点**：美方终战方案 9 月中下旬（是否含制裁缓解条款）；9/11 USDA 作物产量×CFTC 小麦拥挤背景、9/11 CPI、9/16 FOMC。
5. **KMI 跟踪**：30.85–32.9 箱体突破方向；$9.6B 积压投产兑现（2027-29）；2025-08 后脱钩股指叙事验证。
6. **STEO 天然气出口**：9 月中旬月度更新后用 parse_steo_ng_export.py 模板刷新（2025 LNG 15.09 → 2026E 17.38 → 2027E 18.61 Bcf/d）。
7. CVS×IHE 后续（如用户要求可整合编号报告对照 66 号/CVS×US10Y 三线闭环）；黑海停火斡旋（窄版海上协议）、俄乌玉米 10 月出口季、厄尔尼诺三重条件跟踪不变。